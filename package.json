{
  "name": "project-api",
  "version": "1.0.0",
  "description": "",
  "homepage": "./client",
  "scripts": {
    "start": "concurrently --kill-others \"cd Server && npm install && node project-api\" \"cd Client && npm install && npm run build\""
  },
  "start": "concurrentlycd Server && npm install && node project-api",
  "heruko": "cd Client && npm install && npm run build",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
